export const SETLAYOUT = (state, layouts) => {
	state.layouts = layouts;
};

export const SETCOMPONENTS = (state, components) => {
	state.componentList = components;
};

export const SETLAYOUTID = (state, layoutid) => {
	state.layoutId = layoutid;
};
