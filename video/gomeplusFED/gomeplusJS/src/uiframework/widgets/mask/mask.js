import BaseClass from '../../base/Class';

class Mask extends BaseClass {
    constructor(options) {
        super(options);
        this._create();
    }
    _create() {

    }
}

export default Mask;